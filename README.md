# PyTorch Implementation of Attributive Masking Learning (AML)

## Introduction
This is the official PyTorch implementation of the Attributive Masking Learning (AML) method.

We introduce a method that trains an attribution model to identify influential tokens in the is in the input for a given language. model’s prediction


By default, datasets size are the original datasets size, unless defined otherwise.


Example of running files options are in runs folder.